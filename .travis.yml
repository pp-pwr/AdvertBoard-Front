matrix:
  include:
    - language: java
      jdk: oraclejdk8
      before_script:
        - cd advert-board-backend

    - language: node_js
      node_js:
        - "stable"
      cache:
        directories:
        - node_modules
      before_script:
        - cd advert-board-frontend
      script:
        - npm test